# 数据连接：建工成本01

## 基本信息

| 项 | 值 |
| --- | --- |
| 连接 ID | `duckdb__建工成本01` |
| 名称 | 建工成本01 |
| 类型 | `duckdb` |
| 描述 | 建工成本01 |
| 主机 | — |
| 端口 | — |
| 数据库 | storage/dataspaces/ce_cost_01.duckdb |
| Schema | — |
| 用户名 | — |
| 关联空间 | — |
| 导出时间 | 2026-06-06T12:30:20.787Z |

## 流程节点引用

在 `flow.json` 中，`database-source`、`database-sink`、`sql-query` 等节点使用 **`connectionId`** 指向本连接：

```json
{
  "type": "database-source",
  "data": {
    "connectionId": "duckdb__建工成本01",
    "output_variable_name": "查询结果",
    "code": { "sql": "SELECT * FROM your_table LIMIT 100" }
  }
}
```

- `connectionId` 必须为 **`ads_connections` 的字符串 id**（见上文「连接 ID」）。
- 表结构详情可拉取各表的「下载表信息」到 `资源/datasources/<连接名>/` 目录。
- 开发时可将本文件 @ 引用给 AI，便于填写 SQL 与节点配置。

## 数据表列表

| 表名 | 类型 |
| --- | --- |
| `tb_cost_report_cell` | TABLE |
| `tb_cost_report_row` | TABLE |
| `tb_cost_report_view_model_definition_item` | TABLE |
| `tb_project_cost_pay_detail` | TABLE |
| `tb_project_cost_unconfirmed_analyse` | TABLE |
| `tb_project_income_outcome_summary` | TABLE |
| `tb_project_indicator` | TABLE |
| `tb_project_major_cost` | TABLE |
| `tb_project_major_indicator_analyse` | TABLE |
| `tb_project_risk_list_analyse` | TABLE |

## 连接配置（已脱敏）

```json
{
  "storage_type": "database",
  "database_path": "storage/dataspaces/ce_cost_01.duckdb",
  "read_only": false
}
```
