# 数据空间：潘达工程

## 基本信息

| 项 | 值 |
| --- | --- |
| 空间 ID | `space__panda_construction` |
| 名称 | 潘达工程 |
| 描述 | — |
| 存储引擎 | clickhouse |
| 语义层 | native |
| 表数量 | 0 |
| 关系数量 | 0 |
| 业务域 | — |
| 状态 | active |
| 导出时间 | 2026-06-08T02:44:02.952Z |

## 流程与开发引用

- **数据空间 ID**：`space__panda_construction`（CLI：`dazi-flow dataspace list` / `dazi data space list`）
- 空间内表可在侧栏展开后「下载表信息」，保存到 `资源/dataspaces/<空间名>/` 下各表 md。
- 流程节点：`dataspace-source` / `dataspace-sink` 使用 **`spaceId`**（本 ID）+ `code.sql`（读取）或 `tableName`（写入）；读外部库用 `database-source` 的 **`connectionId`**。
- 开发时可将本文件 @ 引用给 AI，便于了解可用表与空间上下文。

## 数据表列表

| 显示名 | 物理表名 | 表 ID | 类型 |
| --- | --- | --- | --- |
| （暂无表） | | | |
