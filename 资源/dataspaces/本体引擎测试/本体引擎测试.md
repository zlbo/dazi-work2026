# 数据空间：本体引擎测试

## 基本信息

| 项 | 值 |
| --- | --- |
| 空间 ID | `space__onto_engine_test` |
| 名称 | 本体引擎测试 |
| 描述 | — |
| 存储引擎 | clickhouse |
| 语义层 | native |
| 表数量 | 6 |
| 关系数量 | 10 |
| 业务域 | — |
| 状态 | active |
| 导出时间 | 2026-06-12T16:41:45.035Z |

## 流程与开发引用

- **数据空间 ID**：`space__onto_engine_test`（CLI：`dazi-flow dataspace list` / `dazi data space list`）
- 空间内表可在侧栏展开后「下载表信息」，保存到 `资源/dataspaces/<空间名>/` 下各表 md。
- 流程节点：`dataspace-source` / `dataspace-sink` 使用 **`spaceId`**（本 ID）+ `code.sql`（读取）或 `tableName`（写入）；读外部库用 `database-source` 的 **`connectionId`**。
- 开发时可将本文件 @ 引用给 AI，便于了解可用表与空间上下文。

## 数据表列表

| 显示名 | 物理表名 | 表 ID | 类型 |
| --- | --- | --- | --- |
| 产值事实表 | `fact_output` | `3215e395c987484d` | native |
| 成本事实表 | `fact_cost` | `94a1fe312c7f4b36` | native |
| 成本科目维表 | `dim_cost_type` | `9c37ea22fdbd4755` | native |
| 项目维表 | `dim_project` | `8f5a388d2e3545aa` | native |
| 组织维表 | `dim_org` | `ee6409ce5f62440f` | native |
| 日期维表 | `dim_date` | `d8e720737b65411c` | native |
