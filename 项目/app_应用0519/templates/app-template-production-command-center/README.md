# @dazi/app-template-production-command-center

生产指挥中心（DRAP 页模式模板）

```powershell
cd ..  # 应用项目根（本 README 在 templates/ 子目录时）
pnpm install
pnpm --filter @dazi/app-template-production-command-center run dev
pnpm --filter @dazi/app-template-production-command-center run build
pnpm run dazi-app -- init production-command-center --space space__0519
```
