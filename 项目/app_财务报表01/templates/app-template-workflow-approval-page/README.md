# @dazi/app-template-workflow-approval-page

审批流挂载页（DRAP 页模式模板）

```powershell
cd ..  # 应用项目根（本 README 在 templates/ 子目录时）
pnpm install
pnpm --filter @dazi/app-template-workflow-approval-page run dev
pnpm --filter @dazi/app-template-workflow-approval-page run build
pnpm run dazi-app -- init workflow-approval-page --space space__0519
```
