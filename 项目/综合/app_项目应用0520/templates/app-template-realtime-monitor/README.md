# @dazi/app-template-realtime-monitor

实时监控卡片墙（DRAP 页模式模板）

```powershell
cd ..  # 应用项目根（本 README 在 templates/ 子目录时）
pnpm install
pnpm --filter @dazi/app-template-realtime-monitor run dev
pnpm --filter @dazi/app-template-realtime-monitor run build
pnpm run dazi-app -- init realtime-monitor --space space__0519
```
