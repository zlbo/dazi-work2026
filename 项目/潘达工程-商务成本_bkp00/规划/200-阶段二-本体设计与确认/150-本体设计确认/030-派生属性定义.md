# 派生属性定义

> **版本**：V2.0  
> **创建日期**：2026年6月6日  
> **更新日期**：2026年6月10日  
> **适用项目**：潘达工程-商务成本智能决策体系  
> **参考**：本体规划文档（V3.0）、阶段二映射分析文档  
> **依据规范**：《本体规划指南》《本体命名规范》
>
> **前置成果**：61个核心实体（V3.0）、企业业务专识（ER001-ER006）
>
> **本次更新**：整合阶段一成果，新增5类派生属性定义（含三色预警、三级成本分类、保证金管理等企业特有派生属性）

---

## 一、派生属性概述

### 1.1 定义说明

**派生属性**（Derived Property）是基于基础属性通过计算、聚合或逻辑运算得到的属性。本定义遵循以下原则：

| 原则 | 说明 |
|------|------|
| **业务导向** | 派生属性应反映业务指标和关键绩效 |
| **可计算性** | 计算逻辑应明确且可重复执行 |
| **一致性** | 同一指标在不同Object Type中的计算逻辑应一致 |
| **知识集成** | 整合阶段一的隐性关系和行业诀窍 |

### 1.2 指标分层体系

| 层级 | 类型 | 说明 | 示例 |
|------|------|------|------|
| L1 | 原始指标 | 直接从数据源获取 | 已确认产值、已确认成本 |
| L2 | 派生指标 | 基于L1计算 | 利润率、成本率 |
| L3 | 复合指标 | 基于L2计算 | 综合得分、健康度 |
| L4 | 决策指标 | 基于L3和业务规则 | 预警等级、决策建议 |

---

## 二、Project派生属性

### 2.1 核心财务指标

| 属性名 | 数据类型 | 计算公式 | 说明 | 关联知识 |
|--------|---------|---------|------|---------|
| **profitRate** | DECIMAL(18,4) | (outputTotal - costConfirmedAcc) / outputTotal * 100 | 项目毛利率(%) | R002,T003 |
| **costRatio** | DECIMAL(18,4) | costConfirmedAcc / outputTotal * 100 | 成本占比(%) | R003,T002 |
| **outputConfirmedRatio** | DECIMAL(18,4) | outputConfirmed / outputTotal * 100 | 产值确认率(%) | R001,T001 |
| **costRigidity** | DECIMAL(18,4) | laborCostAcc / costConfirmedAcc * 100 | 成本刚性度(%) | R001,T002 |

### 2.2 资金指标

| 属性名 | 数据类型 | 计算公式 | 说明 | 关联知识 |
|--------|---------|---------|------|---------|
| **cashBalance** | DECIMAL(18,2) | receivedAmount - paidAmount | 资金结余 | R002 |
| **collectionRate** | DECIMAL(18,4) | receivedAmount / outputConfirmed * 100 | 回款率(%) | R002,T003 |
| **paymentRate** | DECIMAL(18,4) | paidAmount / payableConfirmed * 100 | 付款率(%) | R002,T003 |

### 2.3 风险指标

| 属性名 | 数据类型 | 计算公式 | 说明 | 关联知识 |
|--------|---------|---------|------|---------|
| **riskScore** | INTEGER | SUM(riskValue * weight) for all risks | 风险综合得分 | E008 |
| **healthScore** | DECIMAL(18,4) | (profitRate/20 + collectionRate/100 + confirmedRatio/100) / 3 * 100 | 项目健康度(%) | E007,E008 |

---

## 三、ProjectOutput派生属性

### 3.1 产值分析指标

| 属性名 | 数据类型 | 计算公式 | 说明 | 关联知识 |
|--------|---------|---------|------|---------|
| **outputCurrent** | DECIMAL(18,2) | outputConfirmed + outputUnconfirmed - outputLastYearConfirmed - outputLastYearUnconfirmed | 本年累计产值 | E006 |
| **outputGrowth** | DECIMAL(18,4) | outputCurrent / (outputLastYearConfirmed + outputLastYearUnconfirmed) * 100 - 100 | 同比增长率(%) | E006 |
| **unconfirmedRatio** | DECIMAL(18,4) | outputUnconfirmed / outputTotal * 100 | 待确认比例(%) | R001,T001 |

---

## 四、ProjectCost派生属性

### 4.1 成本分析指标

| 属性名 | 数据类型 | 计算公式 | 说明 | 关联知识 |
|--------|---------|---------|------|---------|
| **costTotalAcc** | DECIMAL(18,2) | costConfirmedAcc + costUnconfirmedAcc | 累计总成本 | E005 |
| **costCurrent** | DECIMAL(18,2) | costConfirmedCmonth + costUnconfirmedCmonth | 本月总成本 | E005 |
| laborRatio | DECIMAL(18,4) | laborCostAcc / costConfirmedAcc | 人工费占比 | ER002 |
| materialRatio | DECIMAL(18,4) | materialCostAcc / costConfirmedAcc | 材料费占比 | ER002 |
| equipmentRatio | DECIMAL(18,4) | equipmentCostAcc / costConfirmedAcc | 设备费占比 | ER002 |

### 4.2 三级成本分类指标（企业特有：ER002）

| 属性名 | 数据类型 | 计算公式 | 说明 | 关联知识 |
|--------|---------|---------|------|---------|
| **L1CostAmount** | DECIMAL(18,2) | SUM(costAmount) WHERE costLevel = 'L1' | 主控费用汇总 | ER002 |
| **L2CostAmount** | DECIMAL(18,2) | SUM(costAmount) WHERE costLevel = 'L2' | 分项费用汇总 | ER002 |
| **L3CostAmount** | DECIMAL(18,2) | SUM(costAmount) WHERE costLevel = 'L3' | 明细费用汇总 | ER002 |
| **costDeviationRate** | DECIMAL(18,4) | (actualCost - targetCost) / targetCost | 成本偏差率 | ER005 |

### 4.3 成本偏差指标

| 属性名 | 数据类型 | 计算公式 | 说明 | 关联知识 |
|--------|---------|---------|------|---------|
| **costVariance** | DECIMAL(18,2) | costConfirmedAcc - targetCost | 成本偏差 | R001 |
| **costVarianceRatio** | DECIMAL(18,4) | costVariance / targetCost * 100 | 成本偏差率(%) | R001 |

---

## 五、ProjectPayment派生属性

### 5.1 付款分析指标

| 属性名 | 数据类型 | 计算公式 | 说明 | 关联知识 |
|--------|---------|---------|------|---------|
| **unpaidAmount** | DECIMAL(18,2) | payableConfirmed - paidAmount | 未付款金额 | R002 |
| **unpaidRatio** | DECIMAL(18,4) | unpaidAmount / payableConfirmed * 100 | 未付款比例(%) | R002 |
| **paymentProgress** | DECIMAL(18,4) | paidAmount / contractAmount * 100 | 付款进度(%) | E004,T003 |
| **laborPaymentRatio** | DECIMAL(18,4) | laborPayable / payableConfirmed * 100 | 人工费占应付款比例(%) | T002 |

---

## 六、ProjectBalance派生属性

### 6.1 收支分析指标

| 属性名 | 数据类型 | 计算公式 | 说明 | 关联知识 |
|--------|---------|---------|------|---------|
| **balanceAmount** | DECIMAL(18,2) | projectAmount - companyAmount | 收支差额 | E007 |
| **incomeRatio** | DECIMAL(18,4) | SUM(收入科目) / totalAmount * 100 | 收入占比(%) | E007 |
| **expenseRatio** | DECIMAL(18,4) | SUM(支出科目) / totalAmount * 100 | 支出占比(%) | E007 |

---

## 七、ProjectIndicator派生属性

### 7.1 指标分析指标

| 属性名 | 数据类型 | 计算公式 | 说明 | 关联知识 |
|--------|---------|---------|------|---------|
| **varianceValue** | DECIMAL(18,4) | indicatorValue - targetValue | 指标偏差值 | E007 |
| **varianceRatio** | DECIMAL(18,4) | varianceValue / targetValue * 100 | 指标偏差率(%) | E007 |
| **isWarning** | BOOLEAN | ABS(varianceRatio) > 10 | 是否预警 | E008 |

---

## 八、ProjectRisk派生属性（三色预警：ER003）

### 8.1 风险预警指标

| 属性名 | 数据类型 | 计算公式 | 说明 | 关联知识 |
|--------|---------|---------|------|---------|
| **greenRiskCount** | INTEGER | COUNT WHERE warningLevel = '绿' | 绿色预警数量 | ER003 |
| **yellowRiskCount** | INTEGER | COUNT WHERE warningLevel = '黄' | 黄色预警数量 | ER003 |
| **redRiskCount** | INTEGER | COUNT WHERE warningLevel = '红' | 红色预警数量 | ER003 |
| **redRiskImmediateReport** | BOOLEAN | redRiskCount > 0 | 红色预警立即上报 | ER003 |
| **totalRiskScore** | INTEGER | SUM(riskValue) WHERE warningLevel | 风险综合得分 | E052 |

---

## 九、Bond派生属性（保证金管理）

### 9.1 保证金分析指标

| 属性名 | 数据类型 | 计算公式 | 说明 | 关联知识 |
|--------|---------|---------|------|---------|
| **unreturnedRatio** | DECIMAL(18,4) | unreturnedAmount / bondAmount | 未退比例 | E014, E015 |
| **forfeitRatio** | DECIMAL(18,4) | forfeitAmount / bondAmount | 没收比例 | E013, E014 |
| **overdueDays** | INTEGER | currentDate - dueDate | 逾期天数 | E014, E015 |
| **returnProgress** | DECIMAL(18,4) | returnedAmount / bondAmount | 退还进度 | E013-E016 |

---

## 十、ProjectOutput派生属性（产值双轨制：ER001）

### 10.1 产值确认指标

| 属性名 | 数据类型 | 计算公式 | 说明 | 关联知识 |
|--------|---------|---------|------|---------|
| **confirmedRatio** | DECIMAL(18,4) | outputConfirmed / outputTotal | 确认率（产值双轨制） | ER001 |
| **unconfirmedRatio** | DECIMAL(18,4) | outputUnconfirmed / outputTotal | 待确认率 | ER001 |
| **outputCurrent** | DECIMAL(18,2) | outputConfirmed + outputUnconfirmed - outputLastYearConfirmed - outputLastYearUnconfirmed | 本年累计产值 | E021 |
| **outputGrowth** | DECIMAL(18,4) | outputCurrent / (outputLastYearConfirmed + outputLastYearUnconfirmed) * 100 - 100 | 同比增长率 | E021 |

---

## 十一、计算规则约束

### 8.1 数值约束

| 约束类型 | 适用属性 | 约束条件 | 处理方式 |
|---------|---------|---------|---------|
| **非负约束** | 金额类属性 | >= 0 | 异常标记 |
| **比例约束** | 比例类属性 | 0-100 | 截断处理 |
| **分母不为零** | 比率计算 | 分母 != 0 | 返回NULL |

### 8.2 时间约束

| 约束类型 | 说明 |
|---------|------|
| **期间一致性** | 参与计算的属性应属于同一报告期间 |
| **累计计算** | 累计值应包含历史期间数据 |

### 8.3 业务规则约束

| 规则编号 | 规则描述 | 适用场景 |
|---------|---------|---------|
| BR001 | 利润率应在-100%到100%之间 | 所有利润率计算 |
| BR002 | 成本占比不应超过100% | 成本比率计算 |
| BR003 | 待确认金额不应超过总额 | 确认率计算 |

---

## 九、派生属性与AI助手问答映射

### 9.1 问答场景映射

| 问答场景 | 关联派生属性 | 示例问题 |
|---------|-------------|---------|
| **指标查询** | profitRate, costRatio | "本月利润率是多少？" |
| **趋势分析** | outputGrowth, costVarianceRatio | "成本同比增长了多少？" |
| **根因分析** | unpaidRatio, unconfirmedRatio | "为什么回款率低？" |
| **风险评估** | riskScore, healthScore | "项目健康度如何？" |

---

**版本**：V1.0  
**创建日期**：2026年6月6日  
**适用项目**：潘达工程-商务成本智能决策体系